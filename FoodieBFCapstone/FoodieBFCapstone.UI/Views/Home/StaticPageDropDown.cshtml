@model FoodieBFCapstone.UI.Models.AdminVM

<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Travels<span class="caret"></span></a>
    <ul class="dropdown-menu" role="menu">
        @foreach (var item in Model.ListStaticPage)
        {
            <li>@Html.ActionLink(@item.Title, "Static", "Home", new { id = @item.AdminPageId }, null)</li>
        }

    </ul>
</li>
